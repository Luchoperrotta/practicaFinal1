<%= form_with(model: current_account) do |form| %>
  <% if current_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(current_account.errors.count, "error") %> prohibited this current_account from being saved:</h2>

      <ul>
        <% current_account.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_account_id %>
    <%= form.collection_select(:user_account_id, UserAccount.all,:id, :accountName) %>

  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :debit %>
    $ <%= form.text_field :debit %>
  </div>

  <div class="field">
    <%= form.label :credit %>
    $ <%= form.text_field :credit %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
